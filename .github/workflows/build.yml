name: Universal Codeberg Builder (GitHub Runner)

on:
  repository_dispatch:
    types: [codeberg-webhook]
  workflow_dispatch:
    inputs:
      codeberg_repo:
        description: 'Codeberg repository (username/repo)'
        required: true
      git_ref:
        description: 'Git ref (tag, branch, or commit)'
        required: true

env:
  CODEBERG_USERNAME: rXelelo

jobs:
  detect-and-build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Parse Trigger Data
        id: parse
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            REPO="${{ github.event.client_payload.repository.full_name }}"
            REF="${{ github.event.client_payload.ref }}"
          else
            REPO="${{ github.event.inputs.codeberg_repo }}"
            REF="${{ github.event.inputs.git_ref }}"
          fi
          
          # Check for tag
          if [[ "$REF" == refs/tags/* ]]; then
            TAG="${REF#refs/tags/}"
            echo "is_tag=true" >> $GITHUB_OUTPUT
            echo "tag=$TAG" >> $GITHUB_OUTPUT
          else
            echo "is_tag=false" >> $GITHUB_OUTPUT
          fi
          
          echo "repo=$REPO" >> $GITHUB_OUTPUT
          echo "repo_name=${REPO##*/}" >> $GITHUB_OUTPUT

      - name: Clone Codeberg Source
        if: steps.parse.outputs.is_tag == 'true'
        run: |
          # Use Token for Auth to handle private repos
          git clone --depth 1 --branch ${{ steps.parse.outputs.tag }} \
            "https://oauth2:${{ secrets.CODEBERG_TOKEN }}@codeberg.org/${{ steps.parse.outputs.repo }}.git" source

      - name: Build & Sign PKGBUILD (Inside Arch Container)
        if: steps.parse.outputs.is_tag == 'true'
        run: |
          cd source
          if [ ! -f "PKGBUILD" ]; then echo "No PKGBUILD, skipping Arch build"; exit 0; fi

          docker run --rm \
            -v $(pwd):/workspace \
            -w /workspace \
            -e GPG_PRIVATE_KEY="${{ secrets.GPG_PRIVATE_KEY }}" \
            -e GPG_PASSPHRASE="${{ secrets.GPG_PASSPHRASE }}" \
            archlinux:base-devel \
            bash -c '
              pacman -Syu --noconfirm gnupg sudo
              useradd -m builder && echo "builder ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
              chown -R builder:builder /workspace
              
              # Force GPG to allow loopback passphrase entry
              su builder -c "mkdir -p ~/.gnupg && echo \"allow-loopback-pinentry\" > ~/.gnupg/gpg-agent.conf"
              su builder -c "echo \"pinentry-mode loopback\" > ~/.gnupg/gpg.conf"
              
              # Import Key
              echo "$GPG_PRIVATE_KEY" | su builder -c "gpg --batch --import"
              
              # Build
              su builder -c "makepkg -sf --noconfirm"
              
              # Sign without TTY (The Fix)
              for pkg in *.pkg.tar.zst; do
                [[ "$pkg" == *"-debug-"* ]] && continue
                echo "Signing $pkg..."
                echo "$GPG_PASSPHRASE" | su builder -c "gpg --batch --yes --pinentry-mode loopback --passphrase-fd 0 --detach-sign --armor $pkg"
              done
            '
          
          mkdir -p ../dist
          cp *.pkg.tar.zst *.pkg.tar.zst.asc ../dist/ 2>/dev/null || true
          rm -f ../dist/*-debug-*

      - name: Create release & Upload to Codeberg
        if: steps.parse.outputs.is_tag == 'true'
        run: |
          REPO="${{ steps.parse.outputs.repo }}"
          TAG="${{ steps.parse.outputs.tag }}"
          API_URL="https://codeberg.org/api/v1/repos/$REPO/releases"
          AUTH="Authorization: token ${{ secrets.CODEBERG_TOKEN }}"

          # 1. Create or Find the Release
          echo "Creating/Finding release for $TAG..."
          RELEASE_JSON=$(curl -s -X POST "$API_URL" \
            -H "$AUTH" -H "Content-Type: application/json" \
            -d "{\"tag_name\":\"$TAG\",\"name\":\"Release $TAG\",\"body\":\"Automated Build\"}")
          
          RELEASE_ID=$(echo "$RELEASE_JSON" | jq -r '.id')
          
          if [ "$RELEASE_ID" == "null" ] || [ -z "$RELEASE_ID" ]; then
            echo "Release exists, fetching ID..."
            RELEASE_ID=$(curl -s -H "$AUTH" "$API_URL/tags/$TAG" | jq -r '.id')
          fi
          
          echo "Using Release ID: $RELEASE_ID"

          # 2. Upload Artifacts
          for file in dist/*; do
            [ -e "$file" ] || continue
            FILENAME=$(basename "$file")
            echo "Uploading $FILENAME (Size: $(du -h "$file" | cut -f1))..."
            
            # Using -T (upload) with --retry and --keepalive for stability
            # Removed --buffer-size to fix the Exit Code 2 error
            curl -v -X POST "$API_URL/$RELEASE_ID/assets?name=$FILENAME" \
              -H "$AUTH" \
              -H "Content-Type: application/octet-stream" \
              -H "Expect: " \
              --retry 5 \
              --retry-delay 10 \
              --connect-timeout 60 \
              --no-buffer \
              --keepalive-time 60 \
              -T "$file"
              
            echo "✅ Successfully uploaded $FILENAME"
          done
      - name: Generate GitHub Summary
        if: always()
        run: |
          echo "### 📦 Codeberg Build Result" >> $GITHUB_STEP_SUMMARY
          echo "Repo: ${{ steps.parse.outputs.repo }}" >> $GITHUB_STEP_SUMMARY
          echo "Tag: ${{ steps.parse.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "Status: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
